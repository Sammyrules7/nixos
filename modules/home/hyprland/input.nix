{
  wayland.windowManager.hyprland.settings = {
    input = {
      kb_layout = "us";
      #kb_variant =
      #kb_model =
      #kb_options =
      #kb_rules =
      follow_mouse = 1;

      touchpad = {
        natural_scroll = true;
      };
    };

    gesture = "4, horizontal, workspace";

    # Example per-device config
    #device {
    #  name = epic-mouse-v1
    #  sensitivity = -0.5
    #};
  };
}
